---
title: Python 빠른 시작
sidebar_label: Python
description: "Python 프로젝트에 Convex 추가하기"
hide_table_of_contents: true
sidebar_position: 500
---

import sampleData from "!!raw-loader!@site/../demos/python-quickstart/sampleData.jsonl";
import tasks from "!!raw-loader!@site/../demos/python-quickstart/convex/tasks.js";
import main from "!!raw-loader!@site/../demos/python-quickstart/main.py";

Python 앱에서 Convex 데이터를 쿼리하는 방법을 알아보세요.

<StepByStep>
  <Step title="Python 스크립트 폴더 생성하기">
    가상 환경이 있는 Python 스크립트 폴더를 생성하세요.

    ```sh
    python3 -m venv my-app/venv
    ```

  </Step>
  <Step title="Convex 클라이언트 및 서버 라이브러리 설치하기">
    시작하려면 백엔드를 작성할 수 있게 해주는 `convex` npm 패키지를 설치하세요.

    또한 `convex` Python 클라이언트 라이브러리와 `.env` 파일 작업을 위한 `python-dotenv`를 설치하세요.

    ```sh
    cd my-app && npm init -y && npm install convex && venv/bin/pip install convex python-dotenv
    ```

  </Step>
  <Step title="Convex 개발 배포 설정하기">
    다음으로, `npx convex dev`를 실행하세요. 이것은 GitHub로 로그인하고, 프로젝트를 생성하고, 프로덕션 및 배포 URL을 저장하라는 메시지를 표시합니다.

    또한 백엔드 API 함수를 작성할 수 있는 `convex/` 폴더를 생성합니다. `dev` 명령은 계속 실행되면서 함수를 클라우드의 개발 배포와 동기화합니다.


    ```sh
    npx convex dev
    ```

  </Step>

  <Step title="데이터베이스용 샘플 데이터 생성하기">
    새 터미널 창에서 샘플 데이터가 포함된 `sampleData.jsonl` 파일을 생성하세요.

    <Snippet
      source={sampleData}
      title="sampleData.jsonl"
    />

  </Step>

  <Step title="데이터베이스에 샘플 데이터 추가하기">
    이제 프로젝트가 준비되었으므로 `import` 명령을 사용하여 샘플 데이터가 포함된 `tasks` 테이블을 Convex 데이터베이스에 추가하세요.

    ```
    npx convex import --table tasks sampleData.jsonl
    ```

  </Step>

  <Step title="데이터베이스 쿼리 노출하기">
    `convex/` 폴더에 데이터를 로드하는 쿼리 함수가 포함된 새 파일 `tasks.js`를 추가하세요.

    이 파일에서 쿼리 함수를 내보내면 파일과 내보내기 이름으로 명명된 API 함수 `"tasks:get"`이 선언됩니다.

    <Snippet
      source={tasks}
      title="convex/tasks.js"
    />

  </Step>

  <Step title="Convex에서 데이터를 로드하는 스크립트 생성하기">
    새 파일 `main.py`에서 `ConvexClient`를 생성하고 이를 사용하여 `"tasks:get"` API에서 데이터를 가져오세요.

    <Snippet
      source={main}
      title="main.py"
    />

  </Step>

  <Step title="스크립트 실행하기">
      스크립트를 실행하고 직렬화된 작업 목록을 확인하세요.

      ```sh
      venv/bin/python -m main
      ```

  </Step>

</StepByStep>

자세한 내용은 [PyPI의 문서](https://pypi.org/project/convex/)를 참조하세요.
